#lang racket

(define (max-two-numbers a b c)
	(values (max a b c) (
	(if (> a b) a b)
	(if (> a c) a c)
	(if (> b c) b c)
)
(define (sum-squares a b) 
	(+ (* a a) (* b b))
(sum-squares (max-two-numbers a b c))
